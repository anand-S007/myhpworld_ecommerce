# ─── API base URL (optional) ─────────────────────────────────────────────────
# Leave unset in local dev — Vite proxies `/api` and `/uploads` to
# http://localhost:3001 (see vite.config.js).
#
# Set this for staging / production deployments:
# VITE_API_URL=https://api.myhpworld.com
VITE_API_URL=
